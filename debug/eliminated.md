# Eliminated explanations

- Initialization guidance alone fixes initial visibility: contradicted by an
  installed-client mock request capture; guidance appeared only after discovery.
- All repair overhead is graph overhead: contradicted by seven independently
  observed shell-policy rejections in historical stderr, absent from JSONL tool counts.
- MCP structured content always replaces text content before model input:
  contradicted for code-mode calls printing the whole result; both markers arrived.
  Direct MCP and other client versions may follow a different conversion path.
